on:
  rss:
    url: 
      - https://feedx.best/rss/economistp.xml
      - https://feedx.best/rss/infzm.xml
jobs:
  request:
    name: Make a HTTP Request
    runs-on: ubuntu-latest
    steps:
      - name: Make a HTTP Request
        uses: actionsflow/axios@v1
        with:
          url: https://cubox.pro/c/api/save/1os58fsty06nmd
          method: POST
          body: |
            {
              "type":"url",
              "content":"${{https://www.economist.com/singingbarley}}", 
              "title": "${{Gene-editing has created a generation of musical crops}}",
              "folder":"The Economist"
            }
